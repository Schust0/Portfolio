import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Arthur SCHUSTER — Portfolio",
  description:
    "Portfolio d'Arthur SCHUSTER, étudiant 3ème année EPITECH Nancy, Développeur Full-Stack. Recherche un stage du 1er avril au 31 août 2026.",
  openGraph: {
    title: "Arthur SCHUSTER — Développeur Full-Stack",
    description:
      "Étudiant 3ème année EPITECH Nancy. Recherche un stage en développement informatique.",
    type: "website",
    locale: "fr_FR",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="fr" className="dark">
      <head>
        <link rel="preconnect" href="https://cdn.jsdelivr.net" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
